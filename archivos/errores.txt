Revisando tu código, encuentro varios problemas que pueden causar errores o comportamientos inesperados:

Problemas identificados:
1. Configuración duplicada de márgenes en html2pdf
javascript
const opt = {
  margin: [15, 10, 15, 10], // ❌ Margen aquí
  jsPDF: { 
    unit: 'mm', 
    format: 'a4', 
    orientation: 'portrait',
    margin: [15, 10, 15, 15] // ❌ Y también aquí (duplicado)
  },
};
2. Manejo inconsistente de fechas
javascript
const formatDate = (date?: Date) => {
  if (!date) return 'Fecha no disponible';
  return new Date(date).toLocaleDateString('es-ES', { // ❌ new Date() sobre un Date
    // ...
  });
};
3. Dependencia faltante en useEffect
javascript
useEffect(() => {
  // ...
  if (recetas.length > 0) {
    loadDoctorInfo();
  }
}, [recetas]); // ❌ Falta doctoresInfo en las dependencias
4. Falta de manejo de casos vacíos
No hay verificación si recetas está vacío o si hay errores al cargar.

5. Estructura de contenedor inconsistente
Mezclas unidades CSS (794px) con unidades de impresión (297mm).